{{> module_start}}
<!-- CONTENT TABLE // -->
<table align="Left" border="0" cellpadding="0" cellspacing="0" width="60" class="flexibleContainer">
    <tr>
        <td align="Left" valign="top" class="imageContent">
            <img src="{{avatar}}" width="60" class="flexibleImage" style="max-width:200px;" />
        </td>
    </tr>
</table>
<!-- // CONTENT TABLE -->
<!-- CONTENT TABLE // -->
<table align="Right" border="0" cellpadding="0" cellspacing="0" width="480" class="flexibleContainer">
    <tr>
        <td valign="top" class="useractionContainer">
            {{#if title}}
            <table border="0" cellpadding="0" cellspacing="0" width="100%">
                <tr>
                    <td valign="top" class="textTitle">
                        <h3>
                            {{#if url}}<a href="{{url}}">{{/if}}
                            {{title}}
                            {{#if url}}</a>{{/if}}
                        </h3>
                    </td>
                </tr>
            </table>
            {{/if}}
            <table border="0" cellpadding="0" cellspacing="0" width="100%">
                <tr>
                    <td valign="top" class="textContent">
